import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SendDetailsView = () => {
  return (
    <View>
      <Text>SendDetailsView</Text>
    </View>
  );
};

export default SendDetailsView;

const styles = StyleSheet.create({});
